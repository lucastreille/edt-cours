import { Routes } from '@angular/router';
import { CoursesListPage } from './pages/courses-list.page';

export const COURS_ROUTES: Routes = [{ path: '', component: CoursesListPage, title: 'Cours' }];
